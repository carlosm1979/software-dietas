const initialState = {
    // estado inicial
  };
  
export const foodsReducer = (state = initialState, action: any) => {
switch (action.type) {
    // manejo de acciones
    default:
    return state;
}
};
